(set string-esc "¬")
(set comma-at (+ string-esc "@"))

(set strings [])

(def push (a x)
  (. a 'push x))

(def extract-strings (x i)
  (if (id 0 (% i 2))
      x
      (do
        (push strings x)
        (+ string-esc (/ (- i 1) 2)))))

(def cons (x xs)
  (concat (arr x) xs))

(def log (x)
  (. console 'log x)
  x)

(log strings)
(push strings "test")
(push strings "another")
(log strings)

(log (cons 'a [b c]))

(log "\"hello\"")